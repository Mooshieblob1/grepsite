name = "grepsite"
compatibility_date = "2024-07-04"

# Pages build configuration
[build]
command = "pnpm build"
cwd = "."

# Pages configuration
pages_build_output_dir = "dist"

# Pages environment variables and bindings
# Note: For Cloudflare Pages, the SESSION KV binding should be configured 
# in the Cloudflare Dashboard under Settings > Functions > KV namespace bindings
# This file serves as documentation of required bindings

# Required KV namespace: SESSION
# - Binding name: SESSION
# - Used for: Session management and authentication
# - Configure in Cloudflare Dashboard for both preview and production environments
